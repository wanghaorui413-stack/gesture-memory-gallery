{
  "name": "gesture-memory-gallery",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "export:standalone": "npm run build && node scripts/export-standalone.mjs",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.3",
    "three": "^0.179.1"
  },
  "devDependencies": {
    "vite": "^7.1.4"
  }
}
